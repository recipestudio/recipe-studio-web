.modal(id="edit-recipe")
    .modal-content
        .row
            .col.s12.center-align
                h4 Edit Recipe
                p Change the fields below to update your recipe.
        .row
            .col.s12.m8.offset-m2
                form
                    .row
                        .input-field.col.s12.m6
                            input.validate(type="text", id="name")
                            label.active(for="name") Recipe Name
                        .input-field.col.s12.m6
                            textarea.materialize-textarea(id="description")
                            label.active(for="description") Description
                    .row
                        .col.s12
                            ul.ingredients-container.collection.with-header
                                li.collection-header 
                                    h6 Ingredients
                    .row.add-ingredient
                        .col.s11
                            .row
                                .input-field.col.s12.m6
                                    input.autocomplete(type="text", id="ingredients-selector")
                                    label.active(for="ingredients-selector") Add ingredient...
                                .input-field.col.s12.m3
                                    input.validate(type="text", id="ingredient-quantity")
                                    label.active(for="ingredient-quantity") Amount
                                .input-field.col.s12.m3
                                    input.validate(type="text", id="ingredient-units")
                                    label.active(for="ingredient-units") Units
                                input(type="hidden", id="ingredient-id")
                        .col.s1
                            a.btn-floating.btn.waves-effect.waves-light(id="add-ingredient-btn")
                                i.material-icons add
                    .row
                        .input-field.col.s12
                            textarea.materialize-textarea(id="directions")
                            label.active(for="directions") Directions
                    .row
                        .col.s12.center-align
                            a.waves-effect.waves-light.btn(id="new-recipe-save") Save
                            a.waves-effect.waves-light.btn.modal-close Cancel